# 斐波那契数列（递归实现）

> 经典递归入门示例 —— 计算斐波那契数列第 `n` 项，并输出前 `n+1` 项。

## 功能说明

- 输入一个非负整数 `n`。
- 输出斐波那契数列第 `n` 项的值（`F(0)=0, F(1)=1`）。
- 输出从 `F(0)` 到 `F(n)` 的所有项。

## 核心算法（递归）

```c
int fib(int n) {
    if (n == 0) return 0;
    if (n == 1) return 1;
    return fib(n - 1) + fib(n - 2);
}
